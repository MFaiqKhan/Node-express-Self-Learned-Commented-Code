const amount=9

if(amount < 10){
    console.log('small number');
}
else{
    console.log('large number');
}

console.log(`hey it's my first node app!!!`)
// we can also use quotation marks/normal strings " '' " and also can use template strings/tilde " `` " .
/*control + ` to open terminal in node.js which is directly in the folder so 
we don't have to guide cmd terminal as it will waste our time */

